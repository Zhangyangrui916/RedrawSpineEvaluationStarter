# Assets

`public_static_mesh_smoke` is a candidate-visible renderer and interface smoke asset. It is not a hidden evaluation instance and retains the original character RGB only for forward-render validation.

The final case generator must replace these pages with low-information S0 pages, generate S1 only within observation union coverage, and package observations without exposing hidden pages or poses.

